<view>{{skill.name}}</view>
<block wx:for="{{skill.contribute}}" wx:key="*this">
    <view>
        <text>{{item.name}}</text><text>{{item.title}}</text>
    </view>
</block>
<block wx:for="{{node}}" wx:key="*this">
<view class="{{item.style}}">
{{item.style}}
{{item.content}}
</view>
</block>
